$schema: https://azuremlschemas.azureedge.net/latest/sweepJob.schema.json
type: sweep
display_name: amazon_review_sweep
experiment_name: amazon_review_training_experiment

trial:
  code: ../src
  command: >
    python train.py
    --train_data ${{inputs.train_data}}/data.parquet
    --val_data ${{inputs.val_data}}/data.parquet
    --test_data ${{inputs.test_data}}/data.parquet
    --output ${{outputs.model_output}}
    --c ${{search_space.c}}
    --max_iter ${{search_space.max_iter}}
  environment:
    image: mcr.microsoft.com/azureml/openmpi4.1.0-ubuntu20.04
    conda_file: ../env/conda.yml

inputs:
  train_data:
    type: uri_folder
    default: azureml:amazon_review_merged_features_train@latest
  val_data:
    type: uri_folder
    default: azureml:amazon_review_merged_features_val@latest
  test_data:
    type: uri_folder
    default: azureml:amazon_review_merged_features_test@latest

outputs:
  model_output:
    type: uri_folder

compute: azureml:lab4-cluster

sampling_algorithm: random

search_space:
  c:
    type: uniform
    min_value: 0.001
    max_value: 0.1
  max_iter:
    type: choice
    values: [1000, 2000, 3000]

objective:
  primary_metric: val_accuracy
  goal: maximize

limits:
  max_total_trials: 6
  max_concurrent_trials: 2